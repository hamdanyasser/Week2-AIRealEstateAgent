# Optional. In dev you can leave this unset — Vite proxies /predict and /health
# to http://localhost:8000 automatically (see vite.config.js).
# For production builds, point this at the public FastAPI URL.
VITE_API_URL=
